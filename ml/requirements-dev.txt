# Dependensi PENGUJIAN service klasterisasi (dipisah dari runtime).
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest -v --cov=pipeline
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
