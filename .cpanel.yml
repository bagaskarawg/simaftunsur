---
deployment:
  tasks:
    - export DEPLOYPATH=/home/artagan1/simaftunsurwebid
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH
    - /usr/local/bin/php artisan migrate --force --seed
    - /usr/local/bin/php artisan optimize:clear
